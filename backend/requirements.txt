# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # Optional: May fail on some systems (PostgreSQL only)
                        # If installation fails, continue with: pip install -r requirements.txt --ignore-installed psycopg2-binary
                        # Or skip it entirely - SQLite works without it
alembic==1.13.1

# Caching & Performance
redis==5.0.1
hiredis==2.3.2

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Rate Limiting
slowapi==0.1.9

# Configuration
python-dotenv==1.0.0

# Logging & Monitoring
python-json-logger==2.0.7
sentry-sdk[fastapi]==1.39.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.25.2
fakeredis==2.20.1

